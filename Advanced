<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>מדריך ניהול חדרי ישיבות — Exchange Online</title>
<link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;700;900&family=IBM+Plex+Mono:wght@400;600&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0e1a;
    --surface: #111827;
    --surface2: #1a2235;
    --border: #1e3a5f;
    --accent: #0ea5e9;
    --accent2: #38bdf8;
    --accent3: #7dd3fc;
    --green: #22c55e;
    --yellow: #f59e0b;
    --red: #ef4444;
    --text: #e2e8f0;
    --text-muted: #94a3b8;
    --text-dim: #475569;
    --glow: 0 0 30px rgba(14,165,233,0.15);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Heebo', sans-serif;
    background: var(--bg);
    color: var(--text);
    line-height: 1.7;
    overflow-x: hidden;
  }

  /* Animated background */
  body::before {
    content: '';
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background:
      radial-gradient(ellipse 80% 50% at 20% 10%, rgba(14,165,233,0.06) 0%, transparent 60%),
      radial-gradient(ellipse 60% 40% at 80% 80%, rgba(56,189,248,0.04) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }

  /* Grid overlay */
  body::after {
    content: '';
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background-image:
      linear-gradient(rgba(14,165,233,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(14,165,233,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
    z-index: 0;
  }

  /* ---- SIDEBAR ---- */
  .sidebar {
    position: fixed;
    right: 0; top: 0; bottom: 0;
    width: 260px;
    background: rgba(17,24,39,0.95);
    border-left: 1px solid var(--border);
    padding: 0;
    z-index: 100;
    display: flex;
    flex-direction: column;
    backdrop-filter: blur(20px);
  }

  .sidebar-logo {
    padding: 24px 20px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .sidebar-logo-icon {
    width: 36px; height: 36px;
    background: linear-gradient(135deg, var(--accent), #0284c7);
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    flex-shrink: 0;
  }

  .sidebar-logo-text {
    font-size: 13px;
    font-weight: 700;
    color: var(--text);
    line-height: 1.3;
  }

  .sidebar-logo-sub {
    font-size: 11px;
    color: var(--text-muted);
    font-weight: 400;
  }

  .sidebar-nav {
    flex: 1;
    overflow-y: auto;
    padding: 16px 0;
  }

  .nav-section-label {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.15em;
    color: var(--text-dim);
    text-transform: uppercase;
    padding: 8px 20px 4px;
    margin-top: 8px;
  }

  .nav-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 9px 20px;
    font-size: 13px;
    color: var(--text-muted);
    text-decoration: none;
    transition: all 0.2s;
    border-right: 2px solid transparent;
    cursor: pointer;
  }

  .nav-item:hover {
    color: var(--accent2);
    background: rgba(14,165,233,0.06);
    border-right-color: var(--accent);
  }

  .nav-item.active {
    color: var(--accent2);
    background: rgba(14,165,233,0.08);
    border-right-color: var(--accent);
  }

  .nav-num {
    display: inline-flex;
    width: 20px; height: 20px;
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 4px;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    font-family: 'IBM Plex Mono', monospace;
    font-weight: 600;
    color: var(--accent);
    flex-shrink: 0;
  }

  .sidebar-footer {
    padding: 16px 20px;
    border-top: 1px solid var(--border);
    font-size: 11px;
    color: var(--text-dim);
  }

  /* ---- MAIN CONTENT ---- */
  .main {
    margin-right: 260px;
    position: relative;
    z-index: 1;
    min-height: 100vh;
  }

  /* ---- HERO ---- */
  .hero {
    padding: 80px 60px 60px;
    border-bottom: 1px solid var(--border);
    position: relative;
    overflow: hidden;
  }

  .hero-tag {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: rgba(14,165,233,0.1);
    border: 1px solid rgba(14,165,233,0.2);
    border-radius: 100px;
    padding: 4px 12px;
    font-size: 12px;
    color: var(--accent2);
    font-weight: 500;
    margin-bottom: 24px;
    animation: fadeSlide 0.6s ease both;
  }

  .hero-tag::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--green);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(0.8); }
  }

  .hero h1 {
    font-size: clamp(32px, 4vw, 52px);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 16px;
    animation: fadeSlide 0.6s 0.1s ease both;
  }

  .hero h1 span {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent3) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-sub {
    font-size: 16px;
    color: var(--text-muted);
    max-width: 600px;
    animation: fadeSlide 0.6s 0.2s ease both;
    margin-bottom: 32px;
  }

  .hero-meta {
    display: flex;
    gap: 24px;
    animation: fadeSlide 0.6s 0.3s ease both;
    flex-wrap: wrap;
  }

  .hero-badge {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: var(--text-muted);
  }

  .hero-badge-dot {
    width: 8px; height: 8px;
    border-radius: 2px;
  }

  .hero-deco {
    position: absolute;
    left: 60px;
    top: 50%;
    transform: translateY(-50%);
    opacity: 0.05;
    font-size: 180px;
    font-weight: 900;
    color: var(--accent);
    pointer-events: none;
    user-select: none;
    line-height: 1;
  }

  @keyframes fadeSlide {
    from { opacity: 0; transform: translateY(16px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ---- CONTENT ---- */
  .content {
    padding: 0 60px 80px;
  }

  /* ---- SECTION ---- */
  .section {
    padding: 60px 0;
    border-bottom: 1px solid rgba(30,58,95,0.4);
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  .section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .section-header {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    margin-bottom: 36px;
  }

  .section-num {
    width: 44px; height: 44px;
    background: linear-gradient(135deg, rgba(14,165,233,0.2), rgba(14,165,233,0.05));
    border: 1px solid rgba(14,165,233,0.3);
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 15px;
    font-weight: 600;
    color: var(--accent);
    flex-shrink: 0;
    margin-top: 4px;
  }

  .section-title {
    font-size: 26px;
    font-weight: 800;
    line-height: 1.2;
  }

  .section-desc {
    font-size: 14px;
    color: var(--text-muted);
    margin-top: 4px;
  }

  /* ---- INFO BOX ---- */
  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 16px;
    margin-bottom: 32px;
  }

  .info-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 20px;
    display: flex;
    align-items: flex-start;
    gap: 14px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .info-card:hover {
    border-color: rgba(14,165,233,0.4);
    box-shadow: var(--glow);
  }

  .info-card-icon {
    width: 40px; height: 40px;
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 20px;
    flex-shrink: 0;
  }

  .info-card-title {
    font-size: 13px;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 4px;
  }

  .info-card-text {
    font-size: 12px;
    color: var(--text-muted);
    line-height: 1.5;
  }

  /* ---- CALLOUTS ---- */
  .callout {
    border-radius: 10px;
    padding: 16px 20px;
    display: flex;
    gap: 12px;
    margin: 16px 0;
    font-size: 14px;
    line-height: 1.6;
    border: 1px solid;
  }

  .callout-icon { font-size: 18px; flex-shrink: 0; line-height: 1.6; }

  .callout.warning {
    background: rgba(245,158,11,0.06);
    border-color: rgba(245,158,11,0.25);
    color: #fcd34d;
  }

  .callout.info {
    background: rgba(14,165,233,0.06);
    border-color: rgba(14,165,233,0.25);
    color: var(--accent3);
  }

  .callout.success {
    background: rgba(34,197,94,0.06);
    border-color: rgba(34,197,94,0.25);
    color: #86efac;
  }

  .callout.danger {
    background: rgba(239,68,68,0.06);
    border-color: rgba(239,68,68,0.25);
    color: #fca5a5;
  }

  /* ---- STEPS ---- */
  .steps {
    display: flex;
    flex-direction: column;
    gap: 0;
    position: relative;
  }

  .steps::before {
    content: '';
    position: absolute;
    right: 19px;
    top: 40px;
    bottom: 40px;
    width: 2px;
    background: linear-gradient(to bottom, var(--accent), transparent);
  }

  .step {
    display: flex;
    gap: 20px;
    padding: 20px 0;
  }

  .step-num {
    width: 40px; height: 40px;
    background: var(--surface2);
    border: 2px solid var(--accent);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    color: var(--accent);
    flex-shrink: 0;
    position: relative;
    z-index: 1;
  }

  .step-content {
    flex: 1;
    padding-top: 8px;
  }

  .step-title {
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--text);
  }

  .step-desc {
    font-size: 13px;
    color: var(--text-muted);
    margin-bottom: 12px;
  }

  /* ---- CODE BLOCK ---- */
  .code-block {
    background: #0d1117;
    border: 1px solid var(--border);
    border-radius: 10px;
    overflow: hidden;
    margin: 12px 0;
    position: relative;
  }

  .code-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 16px;
    background: rgba(255,255,255,0.03);
    border-bottom: 1px solid var(--border);
  }

  .code-lang {
    font-size: 11px;
    font-family: 'IBM Plex Mono', monospace;
    color: var(--accent);
    font-weight: 600;
    letter-spacing: 0.05em;
  }

  .copy-btn {
    background: rgba(14,165,233,0.1);
    border: 1px solid rgba(14,165,233,0.2);
    color: var(--accent2);
    font-size: 11px;
    padding: 4px 10px;
    border-radius: 6px;
    cursor: pointer;
    font-family: 'Heebo', sans-serif;
    transition: all 0.2s;
  }

  .copy-btn:hover {
    background: rgba(14,165,233,0.2);
    border-color: var(--accent);
  }

  .copy-btn.copied {
    background: rgba(34,197,94,0.15);
    border-color: rgba(34,197,94,0.3);
    color: #86efac;
  }

  code, pre {
    font-family: 'IBM Plex Mono', monospace;
  }

  .code-block pre {
    padding: 16px 20px;
    font-size: 13px;
    line-height: 1.7;
    color: #e2e8f0;
    overflow-x: auto;
    white-space: pre;
  }

  .kw { color: #c084fc; }
  .fn { color: #60a5fa; }
  .str { color: #86efac; }
  .param { color: #fde68a; }
  .comment { color: #475569; font-style: italic; }
  .bool { color: #fb923c; }

  /* ---- TABLE ---- */
  .table-wrap {
    overflow-x: auto;
    border-radius: 10px;
    border: 1px solid var(--border);
    margin: 16px 0;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
  }

  thead tr {
    background: rgba(14,165,233,0.08);
    border-bottom: 1px solid var(--border);
  }

  th {
    padding: 12px 16px;
    font-weight: 700;
    color: var(--accent2);
    text-align: right;
    font-size: 12px;
    letter-spacing: 0.05em;
  }

  td {
    padding: 11px 16px;
    border-bottom: 1px solid rgba(30,58,95,0.3);
    color: var(--text-muted);
  }

  tr:last-child td { border-bottom: none; }

  tr:hover td {
    background: rgba(14,165,233,0.03);
    color: var(--text);
  }

  code.inline {
    background: rgba(14,165,233,0.1);
    border: 1px solid rgba(14,165,233,0.15);
    border-radius: 4px;
    padding: 1px 6px;
    font-size: 12px;
    color: var(--accent2);
  }

  .status-true {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    color: var(--red);
    font-weight: 600;
  }

  .status-false {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    color: var(--green);
    font-weight: 600;
  }

  .status-true::before { content: '●'; font-size: 8px; color: var(--red); }
  .status-false::before { content: '●'; font-size: 8px; color: var(--green); }

  /* ---- FLOW DIAGRAM ---- */
  .flow {
    display: flex;
    align-items: center;
    gap: 0;
    margin: 24px 0;
    flex-wrap: wrap;
    justify-content: center;
  }

  .flow-node {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 14px 18px;
    font-size: 13px;
    font-weight: 600;
    text-align: center;
    transition: all 0.2s;
    min-width: 120px;
  }

  .flow-node:hover {
    border-color: var(--accent);
    box-shadow: var(--glow);
  }

  .flow-node .node-icon { font-size: 22px; display: block; margin-bottom: 6px; }
  .flow-node .node-label { color: var(--text-muted); font-size: 11px; font-weight: 400; margin-top: 3px; }

  .flow-arrow {
    color: var(--accent);
    font-size: 20px;
    padding: 0 8px;
    opacity: 0.5;
  }

  /* ---- SUBHEADINGS ---- */
  h3 {
    font-size: 17px;
    font-weight: 700;
    color: var(--text);
    margin: 28px 0 12px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  h3::before {
    content: '';
    display: inline-block;
    width: 3px;
    height: 18px;
    background: var(--accent);
    border-radius: 2px;
  }

  /* ---- PILL TAGS ---- */
  .tag {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 100px;
    font-size: 11px;
    font-weight: 600;
  }

  .tag-blue { background: rgba(14,165,233,0.12); color: var(--accent2); border: 1px solid rgba(14,165,233,0.2); }
  .tag-green { background: rgba(34,197,94,0.1); color: #86efac; border: 1px solid rgba(34,197,94,0.2); }
  .tag-orange { background: rgba(245,158,11,0.1); color: #fcd34d; border: 1px solid rgba(245,158,11,0.2); }

  /* ---- QUICK REF ---- */
  .quick-ref {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 14px;
    overflow: hidden;
    margin-top: 40px;
  }

  .quick-ref-header {
    background: rgba(14,165,233,0.08);
    border-bottom: 1px solid var(--border);
    padding: 16px 24px;
    font-size: 15px;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .quick-ref-body {
    padding: 20px 24px;
  }

  /* ---- SCROLLBAR ---- */
  ::-webkit-scrollbar { width: 6px; height: 6px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--accent); }

  /* ---- RESPONSIVE ---- */
  @media (max-width: 900px) {
    .sidebar { display: none; }
    .main { margin-right: 0; }
    .hero, .content { padding-left: 24px; padding-right: 24px; }
    .hero { padding-top: 40px; }
    .hero-deco { display: none; }
  }

  .section:last-child { border-bottom: none; }

  p { margin-bottom: 12px; color: var(--text-muted); font-size: 14px; }

  a { color: var(--accent2); text-decoration: none; }
  a:hover { text-decoration: underline; }

  /* Timeline for troubleshooting */
  .ts-steps {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 20px;
  }

  .ts-step {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    transition: border-color 0.2s;
  }

  .ts-step:hover { border-color: rgba(14,165,233,0.35); }

  .ts-step-header {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 20px;
    cursor: pointer;
    user-select: none;
  }

  .ts-step-num {
    width: 28px; height: 28px;
    background: rgba(14,165,233,0.12);
    border: 1px solid rgba(14,165,233,0.25);
    border-radius: 6px;
    display: flex; align-items: center; justify-content: center;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    font-weight: 600;
    color: var(--accent);
    flex-shrink: 0;
  }

  .ts-step-title {
    font-size: 14px;
    font-weight: 700;
    color: var(--text);
    flex: 1;
  }

  .ts-step-body {
    padding: 0 20px 16px;
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* Divider */
  .divider {
    height: 1px;
    background: linear-gradient(to left, transparent, var(--border), transparent);
    margin: 8px 0;
  }
</style>
</head>
<body>

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="sidebar-logo">
    <div class="sidebar-logo-icon">🏢</div>
    <div>
      <div class="sidebar-logo-text">Exchange Online</div>
      <div class="sidebar-logo-sub">ניהול חדרי ישיבות</div>
    </div>
  </div>
  <nav class="sidebar-nav">
    <div class="nav-section-label">תוכן עניינים</div>
    <a class="nav-item active" href="#section1"><span class="nav-num">1</span> רישוי ומידע מקדים</a>
    <a class="nav-item" href="#section2"><span class="nav-num">2</span> הקמת החדר (GUI)</a>
    <a class="nav-item" href="#section3"><span class="nav-num">3</span> הכנת PowerShell</a>
    <a class="nav-item" href="#section4"><span class="nav-num">4</span> ניהול Room Lists</a>
    <a class="nav-item" href="#section5"><span class="nav-num">5</span> בעיית שם המארגן</a>
    <a class="nav-item" href="#section6"><span class="nav-num">⚡</span> פתרון מתקדם</a>
    <div class="nav-section-label">כלים</div>
    <a class="nav-item" href="#quickref"><span class="nav-num">📋</span> סיכום פקודות</a>
  </nav>
  <div class="sidebar-footer">Exchange Online Management v3.9.2</div>
</aside>

<!-- MAIN -->
<main class="main">

  <!-- HERO -->
  <section class="hero">
    <div class="hero-tag">Microsoft Exchange Online</div>
    <h1>ניהול חדרי ישיבות<br><span>Room Resources</span></h1>
    <p class="hero-sub">מדריך מקיף להקמה, תצורה ופתרון תקלות — כולל PowerShell, פורטל הניהול ו-Room Panels</p>
    <div class="hero-meta">
      <div class="hero-badge"><div class="hero-badge-dot" style="background:#22c55e"></div>EXO V3 Module</div>
      <div class="hero-badge"><div class="hero-badge-dot" style="background:#f59e0b"></div>Yealink Panels</div>
      <div class="hero-badge"><div class="hero-badge-dot" style="background:#0ea5e9"></div>Teams Rooms</div>
    </div>
    <div class="hero-deco">RM</div>
  </section>

  <div class="content">

    <!-- SECTION 1 -->
    <section class="section" id="section1">
      <div class="section-header">
        <div class="section-num">01</div>
        <div>
          <div class="section-title">דרישות רישוי ומידע מקדים</div>
          <div class="section-desc">מה נדרש לפני שמתחילים</div>
        </div>
      </div>

      <div class="info-grid">
        <div class="info-card">
          <div class="info-card-icon" style="background: rgba(14,165,233,0.12);">🪪</div>
          <div>
            <div class="info-card-title">רישיון SHARED</div>
            <div class="info-card-text">כל מכשיר פיזי דורש רישיון <strong>Shared Device</strong>. אין צורך ברישיון O365 נפרד.</div>
          </div>
        </div>
        <div class="info-card">
          <div class="info-card-icon" style="background: rgba(34,197,94,0.1);">✅</div>
          <div>
            <div class="info-card-title">ללא O365</div>
            <div class="info-card-text">לא נדרש רישוי Office 365 עבור חדרי ישיבות — רישיון Shared מספיק.</div>
          </div>
        </div>
        <div class="info-card">
          <div class="info-card-icon" style="background: rgba(245,158,11,0.1);">📦</div>
          <div>
            <div class="info-card-title">מקור המודול</div>
            <div class="info-card-text"><a href="https://www.powershellgallery.com/packages/ExchangeOnlineManagement/3.9.2" target="_blank">PowerShell Gallery v3.9.2</a></div>
          </div>
        </div>
      </div>

    </section>

    <!-- SECTION 2 -->
    <section class="section" id="section2">
      <div class="section-header">
        <div class="section-num">02</div>
        <div>
          <div class="section-title">הקמת החדר בפורטל הניהול</div>
          <div class="section-desc">יצירת Room Resource דרך ממשק GUI של Exchange Admin Center</div>
        </div>
      </div>

      <div class="callout warning">
        <span class="callout-icon">⚠️</span>
        <div><strong>חשוב מאוד:</strong> יש לדייק בשם החדר באופן מלא — זהו <strong>שם תצוגה שלא ניתן לשינוי</strong> לאחר היצירה.</div>
      </div>

      <div class="divider"></div>

      <!-- Flow -->
      <div class="flow">
        <div class="flow-node">
          <span class="node-icon">🌐</span>
          Exchange Admin<br>
          <span class="node-label">admin.exchange.microsoft.com</span>
        </div>
        <div class="flow-arrow">←</div>
        <div class="flow-node">
          <span class="node-icon">🚪</span>
          Resources<br>
          <span class="node-label">יצירת Room Resource</span>
        </div>
        <div class="flow-arrow">←</div>
        <div class="flow-node">
          <span class="node-icon">📧</span>
          כתובת מייל<br>
          <span class="node-label">תחת הדומיין</span>
        </div>
        <div class="flow-arrow">←</div>
        <div class="flow-node">
          <span class="node-icon">🔑</span>
          ניהול משתמש<br>
          <span class="node-label">admin.cloud.microsoft</span>
        </div>
      </div>

      <h3>קישורים לפורטלים</h3>
      <div class="info-grid">
        <div class="info-card">
          <div class="info-card-icon" style="background: rgba(14,165,233,0.1);">🏢</div>
          <div>
            <div class="info-card-title">פורטל Exchange Admin</div>
            <div class="info-card-text"><a href="https://admin.exchange.microsoft.com/#/resources" target="_blank">admin.exchange.microsoft.com/#/resources</a><br>יצירת Room Resources</div>
          </div>
        </div>
        <div class="info-card">
          <div class="info-card-icon" style="background: rgba(139,92,246,0.1);">👤</div>
          <div>
            <div class="info-card-title">ניהול משתמשים</div>
            <div class="info-card-text"><a href="https://admin.cloud.microsoft/?#/users" target="_blank">admin.cloud.microsoft/?#/users</a><br>סיסמה, רישיון, שיוך</div>
          </div>
        </div>
      </div>

    </section>

    <!-- SECTION 3 -->
    <section class="section" id="section3">
      <div class="section-header">
        <div class="section-num">03</div>
        <div>
          <div class="section-title">הכנת סביבת PowerShell (EXO V3)</div>
          <div class="section-desc">התקנה, הגדרה והתחברות למודול Exchange Online Management</div>
        </div>
      </div>

      <div class="callout info">
        <span class="callout-icon">ℹ️</span>
        <div>ב-Windows PowerShell רגיל <strong>אין</strong> את הפקודה <code class="inline">Connect-ExchangeOnline</code>. היא מגיעה רק אחרי התקנת המודול <strong>EXO V3</strong>.</div>
      </div>

      <div class="steps">

        <div class="step">
          <div class="step-num">א</div>
          <div class="step-content">
            <div class="step-title">הרשאת הרצת סקריפטים</div>
            <div class="step-desc">מאפשר למערכת להריץ מודולים מ-PowerShell Gallery</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Set-ExecutionPolicy</span> <span class="param">RemoteSigned</span> <span class="param">-Scope</span> <span class="param">CurrentUser</span></pre>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-num">ב</div>
          <div class="step-content">
            <div class="step-title">התקנת מודול Exchange Online</div>
            <div class="step-desc">אם מופיעה שאלה לגבי NuGet — יש לאשר "Yes"</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Install-Module</span> <span class="param">-Name</span> <span class="str">ExchangeOnlineManagement</span>
<span class="comment"># או בקיצור:</span>
<span class="fn">Install-Module</span> <span class="str">ExchangeOnlineManagement</span></pre>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-num">ג</div>
          <div class="step-content">
            <div class="step-title">טעינה והתחברות (Import & Connect)</div>
            <div class="step-desc">טען את המודול ואז התחבר לחשבון האדמין — ענה YES לכל ההנחיות</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="comment"># טעינת המודול</span>
<span class="fn">Import-Module</span> <span class="str">ExchangeOnlineManagement</span>

<span class="comment"># התחברות — חלון GUI יקפוץ</span>
<span class="fn">Connect-ExchangeOnline</span></pre>
            </div>
            <div class="callout info">
              <span class="callout-icon">🖥️</span>
              <div>חלון GUI יקפוץ — יש להתחבר לחשבון <strong>האדמין של Microsoft</strong> שינהל את החדרים.</div>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-num">ד</div>
          <div class="step-content">
            <div class="step-title">בדיקת תקינות ההתקנה</div>
            <div class="step-desc">לוודא שהמודול אכן הותקן בהצלחה</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Get-Module</span> <span class="str">ExchangeOnlineManagement</span> <span class="param">-ListAvailable</span></pre>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- SECTION 4 -->
    <section class="section" id="section4">
      <div class="section-header">
        <div class="section-num">04</div>
        <div>
          <div class="section-title">ניהול רשימות חדרים (Room Lists)</div>
          <div class="section-desc">יצירה, שיוך, בקרה — כל הפקודות במקום אחד</div>
        </div>
      </div>

      <div class="callout warning">
        <span class="callout-icon">📌</span>
        <div><strong>Room List נוצרת רק דרך PowerShell</strong> — לא דרך ה-Admin Center.<br>
        Outlook ו-Teams מציגים רק חדרים שנמצאים בתוך Room List. חדר שלא שויך — <strong>לא יופיע</strong>.</div>
      </div>

      <h3>מהי Room List?</h3>
      <p>Room List היא קבוצה שמכילה חדרי ישיבות לפי מיקום (בניין / קומה). Outlook ו-Teams מציגים רק חדרים שנמצאים בתוך Room List תחת "עיין בכל החדרים" ורשימת החדרים המוצעים.</p>

      <h3>א. יצירת Room List חדשה</h3>
      <div class="code-block">
        <div class="code-header">
          <span class="code-lang">PowerShell</span>
          <button class="copy-btn" onclick="copyCode(this)">העתק</button>
        </div>
        <pre><span class="comment"># יצירת רשימת חדרים בשם IL-Rooms</span>
<span class="fn">New-DistributionGroup</span> <span class="param">-Name</span> <span class="str">"IL-Rooms"</span> <span class="param">-RoomList</span></pre>
      </div>

      <h3>ב. הוספת חדר לרשימה</h3>
      <p>נניח שהחדר נקרא <code class="inline">NewRoom@domain.com</code> ושם הרשימה <code class="inline">IL-Rooms</code>:</p>
      <div class="code-block">
        <div class="code-header">
          <span class="code-lang">PowerShell</span>
          <button class="copy-btn" onclick="copyCode(this)">העתק</button>
        </div>
        <pre><span class="fn">Add-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span> <span class="param">-Member</span> <span class="str">"NewRoom@domain.com"</span></pre>
      </div>

      <div class="callout success">
        <span class="callout-icon">⏱️</span>
        <div><strong>זמן המתנה:</strong> אחרי יצירת חדר חדש, ל-Outlook ול-Teams לוקח <strong>10–30 דקות</strong> עד שהוא מופיע בחיפוש.</div>
      </div>

      <h3>ג. פקודות בקרה וניהול</h3>
      <div class="code-block">
        <div class="code-header">
          <span class="code-lang">PowerShell — פקודות ניהול</span>
          <button class="copy-btn" onclick="copyCode(this)">העתק</button>
        </div>
        <pre><span class="comment"># הצגת כל Room Lists קיימות</span>
<span class="fn">Get-DistributionGroup</span> | <span class="kw">where</span> {$_.RecipientTypeDetails <span class="param">-eq</span> <span class="str">"RoomList"</span>}

<span class="comment"># הצגת חדרים בתוך רשימה</span>
<span class="fn">Get-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span>

<span class="comment"># הסרת חדר מרשימה</span>
<span class="fn">Remove-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span> <span class="param">-Member</span> <span class="str">"RoomName@domain.com"</span></pre>
      </div>

    </section>

    <!-- SECTION 5 -->
    <section class="section" id="section5">
      <div class="section-header">
        <div class="section-num">05</div>
        <div>
          <div class="section-title">פתרון בעיית "שם המארגן"</div>
          <div class="section-desc">אי-הופעת שם האירוע — הסיבה והתיקון</div>
        </div>
      </div>

      <div class="callout success">
        <span class="callout-icon">✅</span>
        <div><strong>זה לא באג — זו הגדרה, וניתן לתקן.</strong> ברירת המחדל של Exchange Online היא שחדרי ישיבות מסתירים את נושא הפגישה ומציגים רק את שם המארגן — מטעמי פרטיות.</div>
      </div>

      <h3>הגדרות הגורמות לבעיה</h3>

      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>הגדרה</th>
              <th>ברירת מחדל</th>
              <th>השפעה</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><code class="inline">DeleteSubject</code></td>
              <td><span class="status-true">True</span></td>
              <td>מוחק את שם הפגישה ומחליף בשם המארגן</td>
            </tr>
            <tr>
              <td><code class="inline">AddOrganizerToSubject</code></td>
              <td><span class="status-true">True</span></td>
              <td>מוסיף שם המארגן כנושא הפגישה</td>
            </tr>
            <tr>
              <td><code class="inline">RemovePrivateProperty</code></td>
              <td><span class="status-true">True</span></td>
              <td>מסיר מאפייני פרטיות של הפגישה</td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="steps" style="--step-color: var(--green);">

        <div class="step">
          <div class="step-num" style="border-color: var(--green); color: var(--green);">1</div>
          <div class="step-content">
            <div class="step-title">בדיקת ההגדרות הנוכחיות</div>
            <div class="step-desc">החלף <code class="inline">room@domain.com</code> בכתובת החדר שלך:</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Get-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">room@domain.com</span> | <span class="fn">fl</span> <span class="param">DeleteSubject</span>,<span class="param">AddOrganizerToSubject</span>,<span class="param">RemovePrivateProperty</span></pre>
            </div>
            <p style="margin-top:8px;">אם החדר מציג שם מארגן — תראה:</p>
            <div class="code-block">
              <div class="code-header"><span class="code-lang">תוצאה צפויה (לפני תיקון)</span></div>
              <pre><span style="color:#ef4444">DeleteSubject             : True
AddOrganizerToSubject     : True
RemovePrivateProperty     : True</span></pre>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-num" style="border-color: var(--green); color: var(--green);">2</div>
          <div class="step-content">
            <div class="step-title">תיקון ההגדרות</div>
            <div class="step-desc">שינוי כל שלושת הפרמטרים ל-False:</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Set-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">room@domain.com</span> `
  <span class="param">-DeleteSubject</span> <span class="bool">$False</span> `
  <span class="param">-AddOrganizerToSubject</span> <span class="bool">$False</span> `
  <span class="param">-RemovePrivateProperty</span> <span class="bool">$False</span></pre>
            </div>
            <div class="callout info"><span class="callout-icon">⏱️</span><div>המתן <strong>5–10 דקות</strong> — ה-Room Panel יתעדכן אוטומטית.</div></div>
          </div>
        </div>

        <div class="step">
          <div class="step-num" style="border-color: var(--green); color: var(--green);">3</div>
          <div class="step-content">
            <div class="step-title">וידוא הצלחה</div>
            <div class="step-desc">לאחר ההמתנה, בדוק שהשינוי הוחל:</div>
            <div class="code-block">
              <div class="code-header">
                <span class="code-lang">PowerShell</span>
                <button class="copy-btn" onclick="copyCode(this)">העתק</button>
              </div>
              <pre><span class="fn">Get-CalendarProcessing</span> <span class="str">room@domain.com</span> | <span class="fn">fl</span> <span class="param">DeleteSubject</span>,<span class="param">AddOrganizerToSubject</span></pre>
            </div>
            <div class="code-block">
              <div class="code-header"><span class="code-lang">תוצאה רצויה (אחרי תיקון)</span></div>
              <pre><span style="color:#22c55e">DeleteSubject             : False
AddOrganizerToSubject     : False</span></pre>
            </div>
            <div class="info-grid" style="margin-top:16px;">
              <div class="info-card"><div class="info-card-icon" style="background:rgba(34,197,94,0.1);">✅</div><div><div class="info-card-title">שם הפגישה האמיתי</div><div class="info-card-text">הפאנל יציג את שם הפגישה במקום שם המארגן</div></div></div>
              <div class="info-card"><div class="info-card-icon" style="background:rgba(34,197,94,0.1);">✅</div><div><div class="info-card-title">Outlook / Teams</div><div class="info-card-text">שם הפגישה יוצג גם בלוח השנה של החדר</div></div></div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- SECTION 6 - ADVANCED -->
    <section class="section" id="section6">
      <div class="section-header">
        <div class="section-num">⚡</div>
        <div>
          <div class="section-title">פתרון מתקדם — אם הבעיה נמשכת</div>
          <div class="section-desc">כאשר ה-Calendar Processing עודכן אבל הפאנל עדיין לא תקין</div>
        </div>
      </div>

      <p>אם הגדרות ה-Calendar Processing עודכנו אבל ה-Room Panel עדיין מציג את שם המארגן, יש לבדוק את הנקודות הבאות:</p>

      <div class="ts-steps">

        <div class="ts-step">
          <div class="ts-step-header">
            <div class="ts-step-num">1</div>
            <div class="ts-step-title">לוודא שההגדרות באמת עודכנו</div>
            <span class="tag tag-blue">PowerShell</span>
          </div>
          <div class="ts-step-body">
            <p>הרץ שוב את הפקודה — התוצאה <strong>חייבת</strong> להראות False לכולם:</p>
            <div class="code-block">
              <div class="code-header"><span class="code-lang">PowerShell</span><button class="copy-btn" onclick="copyCode(this)">העתק</button></div>
              <pre><span class="fn">Get-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">ErgoPanel@Ergomeeting.onmicrosoft.com</span> | <span class="fn">fl</span> <span class="param">DeleteSubject</span>,<span class="param">AddOrganizerToSubject</span>,<span class="param">RemovePrivateProperty</span></pre>
            </div>
            <div class="callout danger"><span class="callout-icon">❌</span><div>אם אחד מהם עדיין <code class="inline">True</code> — השינוי <strong>לא הוחל</strong>. יש להריץ שוב את פקודת Set.</div></div>
          </div>
        </div>

        <div class="ts-step">
          <div class="ts-step-header">
            <div class="ts-step-num">2</div>
            <div class="ts-step-title">לוודא שאין Override נוסף</div>
            <span class="tag tag-orange">מתקדם</span>
          </div>
          <div class="ts-step-body">
            <p>יש עוד שתי הגדרות שפחות מוכרות, אבל אם הן מופעלות — הן ממשיכות להסתיר את שם הפגישה:</p>
            <div class="table-wrap" style="margin-bottom:12px;">
              <table>
                <thead><tr><th>הגדרה</th><th>אם True</th></tr></thead>
                <tbody>
                  <tr><td><code class="inline">OrganizerInfo</code></td><td>החדר עדיין מציג שם מארגן</td></tr>
                  <tr><td><code class="inline">DeleteComments</code></td><td>משפיע לפעמים על תצוגת הנושא</td></tr>
                </tbody>
              </table>
            </div>
            <div class="code-block">
              <div class="code-header"><span class="code-lang">PowerShell — בדיקה</span><button class="copy-btn" onclick="copyCode(this)">העתק</button></div>
              <pre><span class="fn">Get-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">ErgoPanel@Ergomeeting.onmicrosoft.com</span> | <span class="fn">fl</span> <span class="param">OrganizerInfo</span>,<span class="param">DeleteComments</span></pre>
            </div>
            <div class="code-block">
              <div class="code-header"><span class="code-lang">PowerShell — תיקון</span><button class="copy-btn" onclick="copyCode(this)">העתק</button></div>
              <pre><span class="fn">Set-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">ErgoPanel@Ergomeeting.onmicrosoft.com</span> `
  <span class="param">-OrganizerInfo</span> <span class="bool">$False</span> `
  <span class="param">-DeleteComments</span> <span class="bool">$False</span></pre>
            </div>
          </div>
        </div>

        <div class="ts-step">
          <div class="ts-step-header">
            <div class="ts-step-num">3</div>
            <div class="ts-step-title">האם הפגישה נוצרה לפני שינוי ההגדרות?</div>
            <span class="tag tag-orange">קריטי</span>
          </div>
          <div class="ts-step-body">
            <div class="callout warning"><span class="callout-icon">⚠️</span><div>פגישות שנוצרו <strong>לפני</strong> שינוי ההגדרות <strong>לא מתעדכנות אוטומטית</strong>. ה-Room Panel ימשיך להציג שם מארגן עבורן.</div></div>
            <p style="margin-top:12px;">כדי לבדוק:</p>
            <div class="steps" style="margin-top:8px;">
              <div style="display:flex; gap:12px; align-items:flex-start; padding: 8px 0;"><span style="color:var(--accent); font-weight:700; font-family: 'IBM Plex Mono', monospace; flex-shrink:0;">1.</span><span style="color:var(--text-muted); font-size:13px;">מחק פגישה קיימת</span></div>
              <div style="display:flex; gap:12px; align-items:flex-start; padding: 8px 0;"><span style="color:var(--accent); font-weight:700; font-family: 'IBM Plex Mono', monospace; flex-shrink:0;">2.</span><span style="color:var(--text-muted); font-size:13px;">צור פגישה <strong>חדשה לגמרי</strong></span></div>
              <div style="display:flex; gap:12px; align-items:flex-start; padding: 8px 0;"><span style="color:var(--accent); font-weight:700; font-family: 'IBM Plex Mono', monospace; flex-shrink:0;">3.</span><span style="color:var(--text-muted); font-size:13px;">בדוק אם הפאנל מציג את שם הפגישה החדש</span></div>
            </div>
            <div class="callout success" style="margin-top:12px;"><span class="callout-icon">✅</span><div>אם הפגישה החדשה מוצגת נכון → הבעיה הייתה בפגישות ישנות בלבד.</div></div>
          </div>
        </div>

        <div class="ts-step">
          <div class="ts-step-header">
            <div class="ts-step-num">4</div>
            <div class="ts-step-title">לוודא שה-Room Panel לא שומר Cache</div>
            <span class="tag tag-blue">Yealink</span>
          </div>
          <div class="ts-step-body">
            <p>פאנלים של Yealink שומרים Cache של לוח השנה. יש לבצע:</p>
            <div style="display:flex; flex-direction:column; gap:8px; margin-top:10px;">
              <div class="info-card"><div class="info-card-icon" style="background:rgba(14,165,233,0.1); font-size:16px;">🔄</div><div><div class="info-card-title">הפעלה מחדש של הפאנל</div></div></div>
              <div class="info-card"><div class="info-card-icon" style="background:rgba(14,165,233,0.1); font-size:16px;">🔌</div><div><div class="info-card-title">ניתוק וחיבור מחדש לחשבון החדר</div></div></div>
              <div class="info-card"><div class="info-card-icon" style="background:rgba(14,165,233,0.1); font-size:16px;">⏳</div><div><div class="info-card-title">המתנה של 5–10 דקות</div></div></div>
            </div>
          </div>
        </div>

        <div class="ts-step">
          <div class="ts-step-header">
            <div class="ts-step-num">5</div>
            <div class="ts-step-title">לוודא שהחדר לא מוגדר כ-"Private Meeting Mode"</div>
            <span class="tag tag-blue">Teams Admin</span>
          </div>
          <div class="ts-step-body">
            <p>יש הגדרה ב-Teams Admin Center שיכולה לגרום לפאנל להציג שם מארגן בלבד — ללא קשר להגדרות Exchange.</p>
            <div class="flow" style="margin: 16px 0; justify-content: flex-start; flex-wrap:wrap; gap:8px;">
              <div class="flow-node" style="min-width: auto; padding:10px 14px; font-size:12px;">Teams Admin Center</div>
              <div class="flow-arrow">←</div>
              <div class="flow-node" style="min-width: auto; padding:10px 14px; font-size:12px;">Devices</div>
              <div class="flow-arrow">←</div>
              <div class="flow-node" style="min-width: auto; padding:10px 14px; font-size:12px;">Panels</div>
              <div class="flow-arrow">←</div>
              <div class="flow-node" style="min-width: auto; padding:10px 14px; font-size:12px;">Settings</div>
              <div class="flow-arrow">←</div>
              <div class="flow-node" style="min-width: auto; padding:10px 14px; font-size:12px; border-color: var(--yellow);">Privacy</div>
            </div>
            <div class="callout warning"><span class="callout-icon">🔍</span><div>
              חפש וכבה את ההגדרות:<br>
              • <code class="inline">Hide meeting titles</code><br>
              • <code class="inline">Show organizer only</code>
            </div></div>
          </div>
        </div>

      </div>
    </section>

    <!-- QUICK REF -->
    <section class="section" id="quickref">
      <div class="quick-ref">
        <div class="quick-ref-header">
          📋 סיכום פקודות PowerShell — Quick Reference
        </div>
        <div class="quick-ref-body">
          <div class="code-block">
            <div class="code-header">
              <span class="code-lang">PowerShell — כל הפקודות</span>
              <button class="copy-btn" onclick="copyCode(this)">העתק הכל</button>
            </div>
            <pre><span class="comment"># ══════════════════════════════════════════
# התחברות
# ══════════════════════════════════════════</span>
<span class="fn">Connect-ExchangeOnline</span>

<span class="comment"># ══════════════════════════════════════════
# ניהול Room Lists
# ══════════════════════════════════════════</span>

<span class="comment"># יצירת Room List</span>
<span class="fn">New-DistributionGroup</span> <span class="param">-Name</span> <span class="str">"IL-Rooms"</span> <span class="param">-RoomList</span>

<span class="comment"># הוספת חדר לרשימה</span>
<span class="fn">Add-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span> <span class="param">-Member</span> <span class="str">"room@domain.com"</span>

<span class="comment"># הצגת כל Room Lists</span>
<span class="fn">Get-DistributionGroup</span> | <span class="kw">where</span> {$_.RecipientTypeDetails <span class="param">-eq</span> <span class="str">"RoomList"</span>}

<span class="comment"># הצגת חדרים ברשימה</span>
<span class="fn">Get-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span>

<span class="comment"># הסרת חדר מרשימה</span>
<span class="fn">Remove-DistributionGroupMember</span> <span class="param">-Identity</span> <span class="str">"IL-Rooms"</span> <span class="param">-Member</span> <span class="str">"room@domain.com"</span>

<span class="comment"># ══════════════════════════════════════════
# Calendar Processing — בדיקה ותיקון
# ══════════════════════════════════════════</span>

<span class="comment"># בדיקה מלאה של הגדרות החדר</span>
<span class="fn">Get-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">room@domain.com</span> | <span class="fn">fl</span> <span class="param">DeleteSubject</span>,<span class="param">AddOrganizerToSubject</span>,<span class="param">RemovePrivateProperty</span>,<span class="param">OrganizerInfo</span>,<span class="param">DeleteComments</span>

<span class="comment"># תיקון מלא — הצגת שם הפגישה</span>
<span class="fn">Set-CalendarProcessing</span> <span class="param">-Identity</span> <span class="str">room@domain.com</span> `
  <span class="param">-DeleteSubject</span> <span class="bool">$False</span> `
  <span class="param">-AddOrganizerToSubject</span> <span class="bool">$False</span> `
  <span class="param">-RemovePrivateProperty</span> <span class="bool">$False</span> `
  <span class="param">-OrganizerInfo</span> <span class="bool">$False</span> `
  <span class="param">-DeleteComments</span> <span class="bool">$False</span></pre>
          </div>
        </div>
      </div>

      <div class="callout info" style="margin-top:24px;">
        <span class="callout-icon">📝</span>
        <div>
          <strong>תזכורות זמנים:</strong><br>
          • לאחר כל שינוי ב-Calendar Processing — המתן <strong>5–10 דקות</strong><br>
          • לאחר יצירת חדר חדש / הוספה לרשימה — המתן <strong>10–30 דקות</strong> להופעה ב-Outlook/Teams
        </div>
      </div>
    </section>

  </div>
</main>

<script>
// Copy button
function copyCode(btn) {
  const pre = btn.closest('.code-block').querySelector('pre');
  const text = pre.innerText;
  navigator.clipboard.writeText(text).then(() => {
    btn.textContent = 'הועתק ✓';
    btn.classList.add('copied');
    setTimeout(() => {
      btn.textContent = 'העתק';
      btn.classList.remove('copied');
    }, 2000);
  });
}

// Scroll spy
const sections = document.querySelectorAll('section[id]');
const navItems = document.querySelectorAll('.nav-item');

const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const id = entry.target.id;
      navItems.forEach(item => {
        item.classList.toggle('active', item.getAttribute('href') === '#' + id);
      });
    }
  });
}, { rootMargin: '-20% 0px -60% 0px' });

sections.forEach(s => observer.observe(s));

// Reveal on scroll
const revealObs = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) e.target.classList.add('visible');
  });
}, { threshold: 0.08 });

document.querySelectorAll('.section').forEach(s => {
  revealObs.observe(s);
});
</script>
</body>
</html>
